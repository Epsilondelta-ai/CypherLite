---
title: Primeiros Passos com Rust
description: Instale e use o CypherLite como um crate Rust com Cargo.
---

import { Callout, Steps, Tabs } from 'nextra/components'

# Primeiros Passos com Rust

CypherLite e uma biblioteca Rust nativa. Adicione-a ao seu projeto com Cargo e comece a trabalhar com dados de grafos em minutos.

## Instalacao

<Steps>

### Adicionar a dependencia

Adicione `cypherlite-query` ao seu `Cargo.toml`:

```toml
[dependencies]
cypherlite-query = "1.2"
```

Para habilitar recursos opcionais:

```toml
[dependencies]
cypherlite-query = { version = "1.2", features = ["temporal-edge", "plugin"] }
```

### Flags de Recursos Disponiveis

| Flag | Description |
|------|-------------|
| `temporal-core` | Temporal graph support (enabled by default) |
| `temporal-edge` | Temporal edge attributes |
| `subgraph` | Subgraph extraction |
| `hypergraph` | Hypergraph support (requires `subgraph`) |
| `full-temporal` | All temporal features |
| `plugin` | Plugin system |

### Compile seu projeto

```bash
cargo build
```

</Steps>

## Uso Basico

### Abrir um Banco de Dados

```rust
use cypherlite_query::CypherLite;

fn main() -> Result<(), Box<dyn std::error::Error>> {
    // Open or create a database file
    let db = CypherLite::open("my_graph.cyl")?;

    // Use the database...

    Ok(())
}
```

<Callout type="info">
O CypherLite cria automaticamente o arquivo do banco de dados se ele nao existir. O arquivo usa a extensao `.cyl` por convencao.
</Callout>

### Criar Nos e Relacionamentos

```rust
// Create nodes with labels and properties
db.execute("CREATE (a:Person {name: 'Alice', age: 30})")?;
db.execute("CREATE (b:Person {name: 'Bob', age: 25})")?;

// Create a relationship between nodes
db.execute(
    "MATCH (a:Person {name: 'Alice'}), (b:Person {name: 'Bob'}) \
     CREATE (a)-[:KNOWS {since: 2023}]->(b)"
)?;
```

### Consultar Dados

```rust
let result = db.execute(
    "MATCH (p:Person) WHERE p.age > 20 RETURN p.name, p.age ORDER BY p.age DESC"
)?;

for row in result {
    let row = row?;
    let name = row.get("p.name").unwrap();
    let age = row.get("p.age").unwrap();
    println!("{}: {}", name, age);
}
```

### Atualizar Propriedades

```rust
db.execute(
    "MATCH (p:Person {name: 'Alice'}) SET p.age = 31"
)?;
```

### Excluir Nos e Relacionamentos

```rust
// Delete a relationship
db.execute(
    "MATCH (a:Person {name: 'Alice'})-[r:KNOWS]->(b:Person {name: 'Bob'}) DELETE r"
)?;

// Delete a node (must have no relationships)
db.execute("MATCH (p:Person {name: 'Bob'}) DELETE p")?;
```

## Operacoes Cypher Suportadas

O CypherLite suporta um subconjunto do openCypher:

- `MATCH` - Pattern matching for nodes and relationships
- `CREATE` - Create nodes and relationships
- `MERGE` - Create if not exists, match if exists
- `SET` - Update properties
- `DELETE` - Remove nodes and relationships
- `RETURN` - Specify output columns
- `WHERE` - Filter conditions
- `WITH` - Chain query parts
- `ORDER BY` - Sort results

## Documentacao da API

A documentacao completa da API Rust esta disponivel em [docs.rs](https://docs.rs/cypherlite-query).

## Requisitos

- **Rust**: 1.84+ (MSRV)
- **Platform**: Linux, macOS, Windows
