[package]
name = "cypherlite-core"
version = "0.1.0"
edition = "2021"
rust-version = "1.84"

[dependencies]
thiserror = "2"
serde = { version = "1", features = ["derive"] }
bincode = "1"

[dev-dependencies]
proptest = "1"
